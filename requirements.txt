# Private Lib — Optional Python Dependencies
# Private_Lib.py uses pure standard library, zero pip install required to run.
# Below are optional dependencies for scripts/ utilities and enhanced features.
# Install: pip install -r requirements.txt

# PDF cover rendering / text extraction
PyMuPDF>=1.22.0

# EPUB deep parsing (batch_import.py)
EbookLib>=0.18.0

# MOBI format parsing
mobi>=0.3.0

# Image processing (cover extraction)
Pillow>=10.0.0
